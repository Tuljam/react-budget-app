import { ReactNode } from "react";

export interface IExpense {
  id: string;
  name: string;
  price: number;
}

export interface IFormData {
  name: string;
  price: number;
}
export interface IFormData {
  name: string;
  price: number;
}
